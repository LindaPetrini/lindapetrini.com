# CV

Place Linda's CV PDF here:

**Filename:** `linda-petrini-cv.pdf`

The contact page links to `/cv/linda-petrini-cv.pdf` with a download prompt.

The `download` attribute on the `<a>` tag means clicking it downloads the file
rather than opening it in the browser.

Update the "Last updated [date]" note in `contact.html` when you upload a new version.
